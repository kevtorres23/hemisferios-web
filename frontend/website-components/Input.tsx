type InputProps = {
    label: string;
    type: "text" | "comment" | "date" | "hour";
}

function Input(props: InputProps) {
    return(
        <div className="flex flex-col gap-2 w-full">

        </div>
    )
}

export default Input;